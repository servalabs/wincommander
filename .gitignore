# Brainstorming session files (visual companion)
.superpowers/

# Agent scratch files + the bundled CLI binary (claude.exe is ~242MB — GitHub
# rejects >100MB, so an accidental `git add -A` push fails for everyone sharing
# this tree). Keep them untracked.
claude.exe
.tscout.txt
.lintout.txt
.anim-rules.txt
.kf-ref.txt
.bp-swap.mjs
.cyan-fix.mjs
mcps/
terminals/

# Dependencies
node_modules/
.pnp
.pnp.js
.npm-cache/

# Lockfiles — repo standardises on Bun (bun.lock); other lockfiles are
# left over from package-manager experimentation and must not be
# committed alongside Bun's lock to avoid drift.
package-lock.json
pnpm-lock.yaml

*.enc

# Per-build encryption salt (generated by encrypt.ts, consumed by build.rs)
.build_salt

# ts-rs codegen scratch — raw output of `cargo test --features ts-codegen`.
# The tracked SSOT copy lives in src/types/generated/ (written by tools/gen-types.ts).
src-tauri/wincmd-shared/bindings/

# Build outputs
dist/
dist-ssr/
*.local
fleet-kit/

# Tauri
src-tauri/target/
src-tauri/target-bench/

# IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local/runtime status dumps
status.json
src-tauri/status.json

# OS files
.DS_Store
Thumbs.db

# Bun
bun.lockb

# Python bytecode caches (generated; never commit)
__pycache__/
*.pyc

# Environment
.env
.env.production
.env.development
.env.staging

# Secrets / certificates / signing keys — defensive denylist so an
# accidental copy never lands in git. None of these are currently
# tracked; the rule prevents future mishaps.
*.pfx
*.p12
*.pem
*.key
*.cer
*.crt
secrets/
private_keys/

website/
# Test coverage
coverage/
other-softwares-refer/
/Change_Log/
.cargo-targetkMOoqm/CACHEDIR.TAG

# Generated feature reference docs (not for repo)
ref/working-features.md
ref/working-features.pdf
ref/features-detailed.md
ref/features.html
ref/features.pdf

# Generated cargo metadata dump (regenerate with `cargo metadata`)
src-tauri/commander-free/metadata.json

# Local secret-scan reports
gitleaks-report.json

# Staging area for commander-pro migration — contains internal endpoints, never commit
staging/

# Cloudflare Wrangler local dev state (SQLite databases, never commit)
cloudflare-license-worker/.wrangler/

# Internal design docs, planning notes, prototypes — never commit
archive/

# Claude Code local config and runtime state — machine-specific, never commit
.claude/

UI_problem/
